export class Rsvp {
  constructor(public first: string, public last: string, public datename: string, public kids: number,  public wedding: number, public reception: number, public brunch: number, public hotel: number) {}
}
